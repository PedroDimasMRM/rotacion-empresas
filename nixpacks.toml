[phases.setup]
nixPkgs = []
aptPkgs = ["fonts-dejavu-core", "fontconfig"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "fc-cache -f 2>/dev/null; streamlit run app/dashboard.py --server.port ${PORT:-8501} --server.address 0.0.0.0 --server.headless true"
